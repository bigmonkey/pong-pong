<% content_for :seotags do %>
<title><%= "Articles for #{@category.topic}" %> | The Payday Hound </title>
<meta name="description" content= "<%= "Save money. Make smart decisions. Catch up on #{@category.topic} and protect yourself while saving money." %>" />
<% end %>

<div id="mainContent">

	<h1>Articles for <%= @category.topic %></h1>
	<p>Save money and make prudent decisions. Our articles and analysis aim to provide fact-based analysis to make it easier to understand borrowing, fees, rates, and payments. Our articles can help you make informed financial decisions to protect yourself and save money. Get the facts so you can make a decision that's best for your situation.</p>

	<% @posts.each do |post| %>
	<article>
		<h1><%= link_to post.title, post_path(post.slug)%></h1>
		<div class = "articleByline">
			Updated <%= post.updated_at.strftime("%B %d, %Y") %>
			<%= render partial:'shared/post/add_categories', locals: {post: post} %>
		</div>
		<div><%= raw(remove_img_tags(post.article).split.first(60).join(" ")) %></div>

		<%# Social Media counters targeted to post url%>
		<!--
		<p>
			<div class = "socialMedia">
				<span class='st_plusone_hcount' displaytext="pluson" st_url= "<%="https://www.thepaydayhound.com/learn/#{post.slug}/" %>" st_title="<%="#{post.title}" %>"></span>
				<span class="st_fblike_hcount" displaytext="facebook" st_url= "<%="https://www.thepaydayhound.com/learn/#{post.slug}/" %>" st_title="<%="#{post.title}" %>"></span>

				<span class='st_twitter_hcount' displaytext="twitter" st_url= "<%="https://www.thepaydayhound.com/learn/#{post.slug}/" %>" st_title="<%="#{post.title}" %>"></span>
				<span class='st_sharethis_hcount' displaytext="sharethis" st_url= "<%="https://www.thepaydayhound.com/learn/#{post.slug}/" %>" st_title="<%="#{post.title}" %>"></span>
			</div>
		</p>
		-->
		<p></p>
	</article>	
	<% end %>

	<%= paginate @posts %>

</div>

<aside id ="sidebarArticle">
<%= render partial:'shared/sidebars/posts_sidebar' %>
	<% # render(:partial => "shared/sidebars/paydayfinder")%>
  <% # render(:partial => "shared/sidebars/sidebar")%>
</aside>